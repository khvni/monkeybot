{
  "name": "@monkeybot/safety",
  "version": "0.1.0",
  "description": "Safety guardrails — app allowlist, confirmation prompts for destructive actions, and kill switch.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
