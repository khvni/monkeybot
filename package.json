{
  "name": "monkeybot",
  "version": "0.1.0",
  "description": "A computer-use agent that learns by watching you work. See, Say, Do.",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
